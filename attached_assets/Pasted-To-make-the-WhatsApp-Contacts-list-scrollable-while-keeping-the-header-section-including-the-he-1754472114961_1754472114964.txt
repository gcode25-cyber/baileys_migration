To make the **WhatsApp Contacts list scrollable** while keeping the header section (including the heading, buttons, select-all checkbox, and search box) sticky, you need to **wrap the contacts list** inside a `div` with scroll behavior and **apply a sticky header** style to the top section.

---

### ✅ **Steps to implement:**

#### 1. **Wrap the contacts list in a scrollable container**

Find this section (in the `contacts` module):

```tsx
{filteredContacts.map((contact: Contact) => (
  <div key={contact.id} className="border rounded-lg p-4 hover:bg-gray-50 dark:hover:bg-gray-800 transition-colors">
    ...
  </div>
))}
```

Wrap it inside a container with a fixed height and `overflow-y-auto`. For example:

```tsx
<div className="h-[calc(100vh-250px)] overflow-y-auto space-y-2 pr-2">
  {filteredContacts.map((contact: Contact) => (
    <div key={contact.id} className="border rounded-lg p-4 hover:bg-gray-50 dark:hover:bg-gray-800 transition-colors">
      ...
    </div>
  ))}
</div>
```

> You can adjust `h-[calc(100vh-250px)]` as needed based on how tall your header is.

---

#### 2. **Make the header sticky**

Find the top header block that looks like this:

```tsx
<div className="mb-6">
  <div className="flex items-center justify-between mb-2">
    ...
  </div>
  ...
</div>
```

Modify it to:

```tsx
<div className="sticky top-0 z-10 bg-gray-50 dark:bg-gray-900 pb-4">
  <div className="flex items-center justify-between mb-2">
    ...
  </div>
  ...
</div>
```

This ensures:

* It's **pinned to the top (`sticky top-0`)**.
* It appears **above the contacts list (`z-10`)**.
* It **doesn't break visual layout (`bg-gray-50`)**.

---

### 🧪 Final layout sample

Here’s a rough structure of what it will look like:

```tsx
<div className="relative">
  {/* Sticky Header */}
  <div className="sticky top-0 z-10 bg-gray-50 dark:bg-gray-900 pb-4">
    {/* Header content including title, buttons, checkbox, search bar */}
  </div>

  {/* Scrollable contacts list */}
  <div className="h-[calc(100vh-250px)] overflow-y-auto space-y-2 pr-2">
    {filteredContacts.map(contact => (
      <div key={contact.id} className="...">
        {/* Contact card */}
      </div>
    ))}
  </div>
</div>
```

---

### ✅ Bonus Tips

* Add `pr-2` to avoid content being hidden behind scrollbar.
* Use `z-10` for header to prevent it from getting overlapped.
* Ensure the parent container (`<div className="p-6">`) doesn’t have `overflow-hidden`.

Would you like me to provide the full code snippet replacing the current `contacts` rendering section?
